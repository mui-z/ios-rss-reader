import XCTest
@testable import RssReaderPackage

final class RssReaderPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(RssReaderPackage().text, "Hello, World!")
    }
}
